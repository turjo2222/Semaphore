---
- name: Create Windows file
  hosts: windows
  gather_facts: no

  tasks:
    - name: Write file
      ansible.windows.win_shell: |
        @"
        Hello from Semaphore.
        File created using PowerShell EOF equivalent.
        "@ | Out-File C:\Semaphore.txt -Encoding UTF8
