---
- name: Copy host file
  hosts: windows
  gather_facts: no

  tasks:
    - name: Copy host.txt
      ansible.windows.win_shell: |
        Copy-Item `
          -Path "\\10.64.105.105\mostafa\host.txt" `
          -Destination "C:\Temp\host.txt" `
          -Force
