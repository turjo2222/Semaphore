---
- name: Windows configuration
  hosts: windows_10
  gather_facts: false
  
  tasks:
    - name: Create folder
      raw: powershell -Command "New-Item -ItemType Directory -Path C:\temp\MyFolder -Force -ErrorAction SilentlyContinue | Out-Null"
      register: create_result
      
    - name: Check if folder exists
      raw: powershell -Command "Test-Path C:\temp\MyFolder"
      register: check_result
      
    - name: Display result
      debug:
        msg: "Folder exists: {{ check_result.stdout }}"
