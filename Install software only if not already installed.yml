---
# ==========================================================
# Installation Policy and Conditions
# ==========================================================
#
# Policy 1:
# Check Windows Registry first to verify whether software
# is already installed.
#
# Policy 2:
# If software is already installed:
#   - Skip copy
#   - Skip installation
#   - Do not modify existing installation
#
# Policy 3:
# If software is NOT installed:
#   - Create C:\Temp if it does not exist
#   - Copy installer from network share
#   - Verify installer file exists
#   - Run silent installation command
#   - Remove installer file after installation
#
# Policy 4:
# Installation will only run when:
#   - Software is missing from the system
#   - Installer file exists in C:\Temp
#
# Policy 5:
# Robocopy exit codes 0-7 are considered successful.
# Only exit codes greater than 7 will fail the task.
#
# Policy 6:
# Software detection checks both:
#   - 64-bit Windows uninstall registry
#   - 32-bit application uninstall registry
#
# ==========================================================


# Playbook: Install software only if not already installed

- name: Copy and install multiple software
  hosts: windows_10
  gather_facts: false

  vars:
    destination: 'C:\Temp'

    software_list:
      - name: Avro Keyboard
        display_name: 'Avro Keyboard'
        src: '\\10.64.105.105\mostafa\Bangla'
        file: 'setup_avrokeyboard_5.6.0.exe'
        install_cmd: 'C:\Temp\setup_avrokeyboard_5.6.0.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART'

      - name: Adobe Reader
        display_name: 'Adobe Reader'
        src: '\\10.64.105.105\mostafa'
        file: 'AdbeRdr11010_en_US.exe'
        install_cmd: 'C:\Temp\AdbeRdr11010_en_US.exe /sAll /rs /msi EULA_ACCEPT=YES'

  tasks:

    # Create C:\Temp if it does not exist
    - name: Create destination folder
      ansible.windows.win_file:
        path: "{{ destination }}"
        state: directory

    # Check if software already exists
    - name: Check if software is already installed
      ansible.windows.win_shell: |
        $app = Get-ItemProperty `
          HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*, `
          HKLM:\Software\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\* `
          -ErrorAction SilentlyContinue |
          Where-Object { $_.DisplayName -like "*{{ item.display_name }}*" }

        if ($app) {
          Write-Output "Installed"
        }
      loop: "{{ software_list }}"
      loop_control:
        label: "{{ item.name }}"
      register: software_status
      changed_when: false

    # Copy installer only when software is missing
    - name: Copy installers using Robocopy
      ansible.windows.win_command: >
        cmd /c "net use \\10.64.105.105\mostafa /user:Guest "" &&
        robocopy {{ item.item.src }} {{ destination }} {{ item.item.file }} /R:3 /W:5 /MT:8"
      loop: "{{ software_status.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      when:
        - "'Installed' not in item.stdout"
      register: copy_result
      failed_when: copy_result.rc > 7

    # Verify installer exists before installation
    - name: Verify installer exists
      ansible.windows.win_stat:
        path: "{{ destination }}\{{ item.item.file }}"
      loop: "{{ software_status.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      register: installer_check
      when:
        - "'Installed' not in item.stdout"

    # Install only if installer exists
    - name: Install software
      ansible.windows.win_command: "{{ item.item.install_cmd }}"
      loop: "{{ installer_check.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      when:
        - item.stat.exists

    # Remove installer after installation
    - name: Remove installers from Temp
      ansible.windows.win_file:
        path: "{{ destination }}\{{ item.item.file }}"
        state: absent
      loop: "{{ installer_check.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      when:
        - item.stat.exists
