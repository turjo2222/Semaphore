---
- name: Copy Avro installer to Windows 10.64.105.50
  hosts: windows_10
  gather_facts: false

  tasks:

    - name: Create temp folder
      raw: |
        powershell.exe -NoProfile -Command "New-Item -ItemType Directory -Path 'C:\temp' -Force"

    - name: Copy file from network share
      raw: |
        powershell.exe -NoProfile -Command "Copy-Item '\\10.64.105.105\mostafa\Bangla\setup_avrokeyboard_5.6.0.exe' -Destination 'C:\temp\setup_avrokeyboard_5.6.0.exe' -Force"

    - name: Verify file copied
      raw: |
        powershell.exe -NoProfile -Command "Test-Path 'C:\temp\setup_avrokeyboard_5.6.0.exe'"
      register: result

    - name: Show result
      debug:
        var: result.stdout
